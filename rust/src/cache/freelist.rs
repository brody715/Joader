pub struct FreeList {
    free_list: Vec<(u64, u64)>,
}

impl FreeList {
    pub fn new() -> FreeList {todo!()}

    pub fn insert(&mut self, start: u64, end: u64) {todo!()}

    pub fn get(&mut self)->(u64, u64) {todo!()}
}